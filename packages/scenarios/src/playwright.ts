/**
 * Entry point for `@mocksmith/scenarios/playwright` — imported from a spec.
 *
 * Named after the subpath it serves, like every other file in this directory;
 * the implementation is in `testing/`.
 * */
export { applyScenario } from './testing/applyScenarioFromPage';
