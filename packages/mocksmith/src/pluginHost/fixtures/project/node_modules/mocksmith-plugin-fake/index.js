/**
 * The package root deliberately exports several unrelated things and no plugin:
 * discovery must use the subpath from the manifest, not this.
 */
export const helperOne = () => 'one';
export const helperTwo = () => 'two';
