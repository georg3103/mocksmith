{
  "name": "mocksmith-example-basic",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "mocksmith start --config ./mocksmith.config.ts",
    "smoke": "node ./smoke.mjs"
  },
  "dependencies": {
    "mocksmith": "workspace:*",
    "@mocksmith/scenarios": "workspace:*",
    "@mocksmith/playwright": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
